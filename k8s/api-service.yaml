apiVersion: v1
kind: Service
metadata:
  name: forecast-api
  namespace: demand-forecast
  labels:
    app: forecast-api
spec:
  type: ClusterIP
  selector:
    app: forecast-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
