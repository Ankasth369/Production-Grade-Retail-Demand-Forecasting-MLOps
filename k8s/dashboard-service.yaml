apiVersion: v1
kind: Service
metadata:
  name: forecast-dashboard
  namespace: demand-forecast
  labels:
    app: forecast-dashboard
spec:
  type: ClusterIP
  selector:
    app: forecast-dashboard
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
